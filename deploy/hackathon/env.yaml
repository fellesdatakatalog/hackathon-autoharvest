---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: halvor
  labels:
    app: halvor
spec:
  template:
    spec:
      containers:
        - name: halvor
          env:
            - name: EXAMPLE_APP_ENV
              value: "hackathon"
#          envFrom:
#            - secretRef:
#                name: example-app
