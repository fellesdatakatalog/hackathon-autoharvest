---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: test4
spec:
  rules:
    - host: ""
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: test4
                port:
                  number: 8080
